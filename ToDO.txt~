1; change the office list

2; 






save change is git add --all
make the change is git commit 
check the history is git log
back to the history is git checkout + id
back to master is git checkout master
push to git repo is git push origin
